#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/sh" ]; then
  "$basedir/sh"  "$basedir/../pre-git/bin/_pre-git-helpers" "$@"
  ret=$?
else 
  sh  "$basedir/../pre-git/bin/_pre-git-helpers" "$@"
  ret=$?
fi
exit $ret
