#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/sh" ]; then
  "$basedir/sh"  "$basedir/../pre-git/bin/post-checkout" "$@"
  ret=$?
else 
  sh  "$basedir/../pre-git/bin/post-checkout" "$@"
  ret=$?
fi
exit $ret
